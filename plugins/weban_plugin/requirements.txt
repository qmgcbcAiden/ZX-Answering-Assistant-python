# 插件依赖配置文件
# Plugin Dependencies Configuration
#
# 说明:
# 1. 每行填写一个依赖包
# 2. 支持标准 pip requirements 格式
# 3. 程序会在加载插件时自动检查并安装这些依赖
# 4. 如果依赖已经安装，会自动跳过
#
# Examples:
# requests>=2.31.0
# beautifulsoup4>=4.12.0
# pandas>=2.0.0
#
# 注意事项:
# - 请使用具体的版本要求（如 >=1.0.0）来确保兼容性
# - 不要包含不必要的依赖，保持最小化
# - 如果插件不需要额外依赖，此文件可以为空或删除

# 此插件目前不需要额外的依赖
# This plugin currently requires no additional dependencies
